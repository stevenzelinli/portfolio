import React from 'react';
import Body from './Body';
import Header from './header/Header';

const Layout = () => (
    <div>
        <Header />
        <Body />
    </div>
);

export default Layout;
